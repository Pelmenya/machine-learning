# data/ — датасеты

- `raw/` — сырые скачанные данные (в .gitignore, в репозиторий не попадают).
- Встроенные датасеты sklearn (Iris, Breast cancer и т.п.) качать не нужно —
  они грузятся прямо из `sklearn.datasets`.

Большие файлы данных не коммитим; если нужен датасет — описываем здесь,
откуда он берётся и как воспроизвести.
